{
  "name": "dat-brain-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Static bilingual AI Engineer portfolio for Phan Trong Dat / Dat Brain.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "preview": "node scripts/preview.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
